pipeline {
    agent any

    stages {
       stage('Hello') {
            steps {
                echo 'Hello World'
                }
       }
       stage('test') {
           steps {
               echo 'Phase de test'
                }
       }
       stage('build') {
           steps {
               echo 'Phase de build'
                }
       }               
       stage('deploy') {
            steps {
                echo 'Déploiement...'
                }
       }
   }
}
